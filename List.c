#include <stdio.h>
#include <stdlib.h>
#include <assert.h>
#include "List.h"


list* create_list(size_t list_size, size_t data_size){

    // Finally, add every function to the function pointers in the list struct

}